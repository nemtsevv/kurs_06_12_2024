<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#f5f5f5">

    <!-- Контейнер для кнопок "Мои автомобили" и "Мои календари" -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center"
        android:layout_marginTop="32dp"
        android:gravity="center">

        <!-- Кнопка "Мои автомобили" -->
        <Button
            android:id="@+id/myCarsButton"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:text="Мои автомобили"
            style="@style/CustomButtonStyle"
            android:layout_marginBottom="16dp"/>

        <!-- Кнопка "Мои календари" -->
        <Button
            android:id="@+id/myCalendarsButton"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:text="Мои календари"
            style="@style/CustomButtonStyle"/>
    </LinearLayout>

    <!-- Кнопка выхода (перемещена в левый нижний угол) -->
    <ImageButton
        android:id="@+id/logoutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_logout"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Logout"
        android:layout_gravity="start|bottom"
        android:layout_marginBottom="16dp"
        android:layout_marginStart="16dp" />

</FrameLayout>
